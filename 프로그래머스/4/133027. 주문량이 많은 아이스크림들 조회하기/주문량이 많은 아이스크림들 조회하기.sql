WITH TOTAL AS (
    SELECT
        *
    FROM
        JULY 
    UNION ALL
    SELECT
        *
    FROM
        FIRST_HALF
)

SELECT
    FLAVOR
FROM
    TOTAL
GROUP BY
    FLAVOR
ORDER BY
    SUM(TOTAL_ORDER) DESC
LIMIT 3